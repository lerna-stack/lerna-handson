# アプリケーションの設定項目を定義する。
example {
  http-api-server {
    host = "127.0.0.1"
    host = ${?APP_HOST}
    port = "9001"
    port = ${?APP_PORT}
    termination-hard-deadline = 10 seconds
  }
  box-office-service {
    response-timeout = 3 seconds
  }
  concert-actor-cluster-sharding {
    entity-type-key-name = "concerts"
  }
}
